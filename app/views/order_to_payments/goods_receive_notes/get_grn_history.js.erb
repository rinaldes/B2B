$("#grn-history").append("<%=escape_javascript(render 'grn_history')%>");
<%if @past_grn.last_page?%>
  $('#history-btn').html("<%= escape_javascript(link_to('No More History','' , :disabled=>'disabled',:class=> 'btn btn-large btn-default' ))%>");
<%else%>
  $('#load-more-history').replaceWith("<%= escape_javascript(link_to_next_page(@past_grn, 'Load More History >>', :remote => true, :id=>'load-more-history', :class=> 'btn btn-large btn-default', params: params))%>");
<%end%>
$(".detail-table").dataTable().fnUpdate();
